import Modes from '@Components/Modes';
import React from 'react';

const ModesPage = () => {
  return (
    <div className="w-full">
      <Modes handleNextClick={() => {}} />
    </div>
  );
};

export default ModesPage;
